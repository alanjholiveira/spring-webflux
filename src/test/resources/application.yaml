spring:
  application:
    name: weflux
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/postgres?schema=anime
    password: root
    username: root

logging:
  level:
    org:
      springframework: INFO

